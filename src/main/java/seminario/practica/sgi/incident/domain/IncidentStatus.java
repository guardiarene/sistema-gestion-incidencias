package seminario.practica.sgi.incident.domain;

public enum IncidentStatus {
    UNASSIGNED,
    ASSIGNED,
    RESOLVING,
    IN_PROGRESS,
    ON_HOLD,
    RESOLVED,
    CLOSED,
    REOPENED,
    CANCELLED
}
